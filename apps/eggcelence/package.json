{
  "name": "eggcelence",
  "version": "1.0.0",
  "description": "A dead-simple egg timer. Tap a preset, walk away, get alerted when it's ready. No build step, no backend, no login — open `index.html` and it works.",
  "main": "sw.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "node sync-www.js && npx cap sync"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@capacitor/local-notifications": "^8.2.1"
  }
}
